export enum TownId {
  Farm = "farm",
}

export enum TownId {
  Plains = "plains",
}

export enum TownId {
  Forest = "forest",
}

export enum TownId {
  Mountains = "mountains",
}

export const TownIds: TownId[] = [
  TownId.Farm,
  TownId.Plains,
  TownId.Forest,
  TownId.Mountains,
];
