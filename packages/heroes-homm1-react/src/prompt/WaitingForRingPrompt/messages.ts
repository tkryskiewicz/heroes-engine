import { defineMessages } from "react-intl";

export const messages = defineMessages({
  message: {
    defaultMessage: "Waiting for ring...",
    id: "game.ui.waitingForRingPrompt.message",
  },
});
