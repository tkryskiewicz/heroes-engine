export { legendImages } from "./legend";

const buttonImages = {
  exit: {
    disabled: require("./exit/disabled.png"),
    enabled: require("./exit/enabled.png"),
  },
};

export {
  buttonImages,
};
