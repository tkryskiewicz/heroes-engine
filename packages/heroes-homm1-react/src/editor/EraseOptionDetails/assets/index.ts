import BackgroundImage = require("./background.jpg");

const typesButton = {
  disabled: require("./types/disabled.png"),
  enabled: require("./types/enabled.png"),
};

export {
  BackgroundImage,
  typesButton,
};
