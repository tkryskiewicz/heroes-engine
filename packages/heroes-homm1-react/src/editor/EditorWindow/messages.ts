import { defineMessages } from "react-intl";

export const messages = defineMessages({
  invalidPlacement: {
    defaultMessage: "Invalid Placement",
    id: "game.ui.editorWindow.invalidPlacement",
  },
});
